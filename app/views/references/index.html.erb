<h1>References</h1>
  <strong><%= link_to "Add References", new_reference_path(current_milieu: @milieu) %></strong>
</p>

<div id="References">
<% @references.each do |ref| %>
      <%= link_to ref.name || ref.entity.name, reference_path(ref.id, current_milieu: @milieu) %>
      <sup><%= ref.eid %></sup></br>
<% end %>
</div>
