<h1><%= @story.title %> </h1>

        <%= link_to "Edit", edit_story_path(@story.id), data: { controller: "tooltip", tooltip_content_value: "A helpful tip for the link" }%>

<div class="container mx-auto px-4">
<p>
<div class = "storytext"><%= markdown(@story.details) %></div>
<p>

<%= button_to "Delete", @story, method: :delete, data: { turbo_confirm: "Are you sure?" } %>