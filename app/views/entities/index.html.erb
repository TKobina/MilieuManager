<h1>Entities</h1>
<div id="entities">
  <ul>
  <% @entities.each do |entity| %>
    <div>
      <li><strong><%= link_to entity.name, entity_path(entity.id, current_milieu: @milieu) %></strong> (<%=entity.kind%>)
      <% if @owner %><sup><%=entity.eid %></sup><%end%> </li>
      <ul>  
      </ul>
    </div>
  <% end %>
  </ul>
</div>
